name: epaxos_revisited_replicated
runtime:
  name: python
  options:
    virtualenv: .venv
description: Testing harness to re-evaluate epaxos
config:
  pulumi:tags:
    value:
      pulumi:template: gcp-python
